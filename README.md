# Idel-cabinet
Transmissions
IDEL Cabinet *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; } :root { --bg: #0f1923; --surface: #161f2c; --surface2: #1d2a3a; --surface3: #243447; --border: rgba(255,255,255,0.07); --border2: rgba(255,255,255,0.12); --accent: #4ecdc4; --accent2: #ff6b6b; --gold: #f0b429; --text: #e8eef4; --muted: #6b8299; --muted2: #4a6278; --recording: #ff4757; --success: #7dd98a; --radius: 12px; } html, body { height: 100%; background: var(--bg); color: var(--text); font-family: 'DM Sans', sans-serif; font-weight: 300; -webkit-font-smoothing: antialiased; overflow: hidden; } /* ── Layout ── */ .app { display: flex; flex-direction: column; height: 100vh; height: 100dvh; } .screen { display: none; flex-direction: column; flex: 1; overflow: hidden; } .screen.active { display: flex; } /* ── Auth screen ── */ .auth-screen { align-items: center; justify-content: center; padding: 32px 24px; gap: 24px; background: radial-gradient(ellipse at 50% 0%, rgba(78,205,196,0.08) 0%, transparent 60%), var(--bg); } .auth-logo { font-family: 'DM Serif Display', serif; font-size: 28px; letter-spacing: -0.5px; color: var(--accent); } .auth-sub { font-size: 13px; color: var(--muted); text-align: center; } .auth-card { width: 100%; max-width: 360px; background: var(--surface); border: 1px solid var(--border); border-radius: 16px; padding: 24px; display: flex; flex-direction: column; gap: 14px; } .auth-tabs { display: flex; border-bottom: 1px solid var(--border); margin-bottom: 4px; } .auth-tab { flex: 1; padding: 10px; text-align: center; font-size: 13px; color: var(--muted); cursor: pointer; border-bottom: 2px solid transparent; transition: all 0.2s; } .auth-tab.active { color: var(--accent); border-bottom-color: var(--accent); } .field-label { font-size: 11px; text-transform: uppercase; letter-spacing: 0.7px; color: var(--muted); margin-bottom: 5px; } input[type=text], input[type=email], input[type=password], input[type=date], textarea { width: 100%; background: var(--bg); border: 1px solid var(--border); border-radius: 8px; padding: 11px 12px; color: var(--text); font-family: 'DM Sans', sans-serif; font-size: 14px; outline: none; transition: border-color 0.2s; } input:focus, textarea:focus { border-color: rgba(78,205,196,0.4); } input::placeholder, textarea::placeholder { color: var(--muted2); } textarea { resize: none; line-height: 1.5; } .btn-primary { width: 100%; padding: 13px; border-radius: 10px; border: none; background: linear-gradient(135deg, var(--accent), #2ecc8a); color: #0f1923; font-family: 'DM Sans', sans-serif; font-size: 14px; font-weight: 500; cursor: pointer; transition: all 0.2s; display: flex; align-items: center; justify-content: center; gap: 8px; } .btn-primary:disabled { opacity: 0.35; cursor: not-allowed; } .btn-primary:not(:disabled):active { transform: scale(0.97); } .btn-secondary { padding: 10px 16px; border-radius: 8px; border: 1px solid var(--border); background: none; color: var(--muted); font-family: 'DM Sans', sans-serif; font-size: 13px; cursor: pointer; transition: all 0.15s; } .btn-secondary:hover { color: var(--text); border-color: var(--border2); } .btn-danger { padding: 8px 14px; border-radius: 8px; border: 1px solid rgba(255,71,87,0.25); background: rgba(255,71,87,0.08); color: var(--accent2); font-family: 'DM Sans', sans-serif; font-size: 12px; cursor: pointer; } .auth-error { font-size: 12px; color: var(--accent2); background: rgba(255,71,87,0.08); border: 1px solid rgba(255,71,87,0.2); border-radius: 8px; padding: 10px 12px; display: none; } /* ── App header ── */ .app-header { padding: 14px 16px; display: flex; align-items: center; gap: 10px; border-bottom: 1px solid var(--border); flex-shrink: 0; } .app-header-title { font-family: 'DM Serif Display', serif; font-size: 17px; color: var(--text); } .app-header-sub { font-size: 11px; color: var(--muted); margin-top: 1px; } .header-back { background: none; border: none; color: var(--muted); cursor: pointer; padding: 4px 8px 4px 0; font-size: 18px; } .header-spacer { flex: 1; } .header-user { font-size: 11px; color: var(--muted); text-align: right; } /* ── Scrollable content ── */ .scroll-content { flex: 1; overflow-y: auto; padding: 16px; display: flex; flex-direction: column; gap: 12px; -webkit-overflow-scrolling: touch; } /* ── Patient list ── */ .section-title { font-size: 11px; text-transform: uppercase; letter-spacing: 0.8px; color: var(--muted); padding: 4px 0 8px; } .patient-card { background: var(--surface); border: 1px solid var(--border); border-radius: var(--radius); padding: 14px 16px; cursor: pointer; transition: all 0.15s; display: flex; align-items: center; gap: 12px; } .patient-card:active { background: var(--surface2); } .patient-avatar { width: 38px; height: 38px; border-radius: 50%; background: linear-gradient(135deg, var(--surface2), var(--surface3)); border: 1px solid var(--border); display: flex; align-items: center; justify-content: center; font-family: 'DM Serif Display', serif; font-size: 15px; color: var(--accent); flex-shrink: 0; } .patient-info { flex: 1; min-width: 0; } .patient-name { font-size: 15px; font-weight: 400; color: var(--text); } .patient-meta { font-size: 12px; color: var(--muted); margin-top: 2px; } .patient-count { background: rgba(78,205,196,0.12); color: var(--accent); font-size: 11px; padding: 3px 8px; border-radius: 20px; flex-shrink: 0; } .fab { position: fixed; bottom: 24px; right: 20px; width: 54px; height: 54px; border-radius: 50%; background: linear-gradient(135deg, var(--accent), #2ecc8a); border: none; color: #0f1923; font-size: 24px; cursor: pointer; box-shadow: 0 4px 20px rgba(78,205,196,0.3); display: flex; align-items: center; justify-content: center; transition: transform 0.2s; z-index: 100; } .fab:active { transform: scale(0.92); } /* ── Modal ── */ .modal-overlay { display: none; position: fixed; inset: 0; background: rgba(0,0,0,0.7); z-index: 200; align-items: flex-end; justify-content: center; } .modal-overlay.open { display: flex; } .modal { background: var(--surface); border-radius: 20px 20px 0 0; width: 100%; max-width: 500px; padding: 20px; display: flex; flex-direction: column; gap: 14px; max-height: 85vh; overflow-y: auto; animation: slideup 0.25s ease; } @keyframes slideup { from { transform: translateY(40px); opacity: 0; } to { transform: translateY(0); opacity: 1; } } .modal-title { font-family: 'DM Serif Display', serif; font-size: 18px; color: var(--text); } .modal-actions { display: flex; gap: 10px; } /* ── Transmission list ── */ .transmission-card { background: var(--surface); border: 1px solid var(--border); border-radius: var(--radius); overflow: hidden; } .tx-header { padding: 12px 14px; display: flex; align-items: center; justify-content: space-between; border-bottom: 1px solid var(--border); cursor: pointer; } .tx-date { font-size: 13px; color: var(--text); } .tx-author { font-size: 11px; color: var(--muted); } .tx-arrow { color: var(--muted); font-size: 12px; transition: transform 0.2s; } .tx-arrow.open { transform: rotate(180deg); } .tx-body { display: none; padding: 14px; gap: 10px; flex-direction: column; } .tx-body.open { display: flex; } .soap-tag { font-size: 10px; letter-spacing: 0.8px; text-transform: uppercase; font-weight: 500; padding: 2px 7px; border-radius: 4px; display: inline-block; margin-bottom: 4px; } .tag-s { background: rgba(78,205,196,0.12); color: var(--accent); } .tag-o { background: rgba(255,165,0,0.1); color: #ffaa33; } .tag-a { background: rgba(255,107,107,0.1); color: #ff9999; } .tag-p { background: rgba(100,200,120,0.12); color: var(--success); } .soap-text { font-size: 13px; line-height: 1.6; color: var(--text); } .soap-divider { height: 1px; background: var(--border); } /* ── Dictée screen ── */ .dictee-screen .scroll-content { align-items: stretch; } .mic-zone { display: flex; flex-direction: column; align-items: center; padding: 20px 0 12px; gap: 12px; } .mic-wrap { position: relative; width: 100px; height: 100px; display: flex; align-items: center; justify-content: center; } .mic-ring { position: absolute; inset: 0; border-radius: 50%; border: 2px solid rgba(78,205,196,0.2); transition: all 0.3s; } .mic-ring.pulse { animation: ringpulse 1.2s ease-in-out infinite; border-color: rgba(255,71,87,0.5); } @keyframes ringpulse { 0%,100%{transform:scale(1);opacity:1}50%{transform:scale(1.25);opacity:0} } .mic-btn { width: 80px; height: 80px; border-radius: 50%; border: none; background: linear-gradient(145deg, var(--surface2), var(--surface)); box-shadow: 0 4px 20px rgba(0,0,0,0.4); cursor: pointer; display: flex; align-items: center; justify-content: center; transition: all 0.2s; z-index: 1; } .mic-btn.recording { background: linear-gradient(145deg, #2d0f0f, #1a0808); box-shadow: 0 4px 28px rgba(255,71,87,0.3); } .mic-btn:active { transform: scale(0.93); } .mic-label { font-size: 13px; color: var(--muted); } .mic-label.active { color: var(--recording); animation: blink 1s infinite; } @keyframes blink { 0%,100%{opacity:1}50%{opacity:0.4} } .transcript-box { background: var(--surface); border: 1px solid var(--border); border-radius: var(--radius); padding: 12px 14px; min-height: 60px; font-size: 14px; line-height: 1.6; color: var(--muted); font-style: italic; transition: border-color 0.2s; } .transcript-box.has-content { color: var(--text); font-style: normal; border-color: rgba(78,205,196,0.2); } .field-group { display: flex; flex-direction: column; gap: 5px; } /* ── Loading ── */ .spinner { width: 16px; height: 16px; border: 2px solid rgba(15,25,35,0.3); border-top-color: #0f1923; border-radius: 50%; animation: spin 0.7s linear infinite; } @keyframes spin { to { transform: rotate(360deg); } } .loading-overlay { display: none; position: fixed; inset: 0; background: rgba(15,25,35,0.8); z-index: 300; align-items: center; justify-content: center; flex-direction: column; gap: 12px; font-size: 13px; color: var(--muted); } .loading-overlay.show { display: flex; } .big-spinner { width: 32px; height: 32px; border: 3px solid rgba(78,205,196,0.2); border-top-color: var(--accent); border-radius: 50%; animation: spin 0.8s linear infinite; } /* ── Toast ── */ .toast { position: fixed; bottom: 90px; left: 50%; transform: translateX(-50%) translateY(20px); background: var(--surface2); border: 1px solid var(--border2); border-radius: 20px; padding: 10px 18px; font-size: 13px; color: var(--text); opacity: 0; transition: all 0.25s; pointer-events: none; white-space: nowrap; z-index: 400; } .toast.show { opacity: 1; transform: translateX(-50%) translateY(0); } /* ── Empty state ── */ .empty-state { display: flex; flex-direction: column; align-items: center; justify-content: center; padding: 40px 20px; gap: 10px; color: var(--muted); font-size: 13px; text-align: center; flex: 1; } .empty-icon { font-size: 36px; opacity: 0.4; } /* ── API config ── */ .api-config-bar { background: rgba(240,180,41,0.08); border: 1px solid rgba(240,180,41,0.2); border-radius: 10px; padding: 10px 14px; font-size: 12px; color: #f0b429; display: flex; align-items: center; gap: 10px; } .api-config-bar button { background: none; border: 1px solid rgba(240,180,41,0.3); color: #f0b429; font-size: 11px; padding: 4px 10px; border-radius: 6px; cursor: pointer; font-family: 'DM Sans', sans-serif; } /* ── Patient detail header ── */ .patient-detail-header { padding: 16px; border-bottom: 1px solid var(--border); background: var(--surface); flex-shrink: 0; display: flex; align-items: center; gap: 12px; } .patient-detail-avatar { width: 44px; height: 44px; border-radius: 50%; background: linear-gradient(135deg, var(--surface2), var(--surface3)); border: 1px solid var(--border); display: flex; align-items: center; justify-content: center; font-family: 'DM Serif Display', serif; font-size: 18px; color: var(--accent); flex-shrink: 0; } .patient-detail-name { font-family: 'DM Serif Display', serif; font-size: 18px; } .patient-detail-meta { font-size: 12px; color: var(--muted); margin-top: 2px; } 

🩺 IDEL Cabinet

Transmissions partagées pour votre cabinet

Connexion

Créer un compte

Email

Mot de passe

Se connecter 

Prénom Nom

Email

Mot de passe

Créer le compte 

🩺 Cabinet

—

Déconnexion 

👤

Aucun patient enregistré.
Appuyez sur + pour en ajouter un.

+ 

← 

?

—

—

+ Dictée 

📋

Aucune transmission.
Commencez par dicter.

← 

Nouvelle transmission

—

⚠️ Clé API Claude manquante Configurer 

Appuyez pour dicter

Transcription

Votre dictée apparaîtra ici…

✦ Générer & Enregistrer 

Nouveau patient

Nom *

Prénom *

Date de naissance

Notes

Annuler Enregistrer 

Clé API Claude

Clé Anthropic (sk-ant-...)

Stockée localement sur votre appareil uniquement. Obtenez la vôtre sur console.anthropic.com

Annuler Enregistrer 

Chargement…

// ── Constants ──────────────────────────────────────────────────────────── const SUPABASE_URL = 'https://mjpeyqiiozlafmtkvros.supabase.co'; const SUPABASE_ANON = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im1qcGV5cWlpb3psYWZtdGt2cm9zIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzU4MTk2ODksImV4cCI6MjA5MTM5NTY4OX0.6WdeicDqpXTE478gQyLdlxtKarRv3HSgMMdoyV6OBXY'; const { createClient } = supabase; const db = createClient(SUPABASE_URL, SUPABASE_ANON); // ── State ──────────────────────────────────────────────────────────────── let currentUser = null; let currentPatient = null; let recognition = null; let isRecording = false; let fullTranscript = ''; let apiKey = localStorage.getItem('idel_api_key') || ''; // ── Init ───────────────────────────────────────────────────────────────── window.addEventListener('DOMContentLoaded', async () => { initSpeech(); if (apiKey) document.getElementById('apiKeyInput').value = apiKey; const { data: { session } } = await db.auth.getSession(); if (session?.user) { currentUser = session.user; afterLogin(); } db.auth.onAuthStateChange((_event, session) => { if (session?.user && !currentUser) { currentUser = session.user; afterLogin(); } }); }); // ── Auth ───────────────────────────────────────────────────────────────── function switchTab(tab) { document.getElementById('tabLogin').classList.toggle('active', tab==='login'); document.getElementById('tabSignup').classList.toggle('active', tab==='signup'); document.getElementById('formLogin').style.display = tab==='login' ? 'flex' : 'none'; document.getElementById('formSignup').style.display = tab==='signup' ? 'flex' : 'none'; document.getElementById('formLogin').style.flexDirection = 'column'; document.getElementById('formLogin').style.gap = '12px'; document.getElementById('formSignup').style.flexDirection = 'column'; document.getElementById('formSignup').style.gap = '12px'; } async function doLogin() { const email = document.getElementById('loginEmail').value.trim(); const pass = document.getElementById('loginPassword').value; const errEl = document.getElementById('loginError'); errEl.style.display = 'none'; if (!email || !pass) { showAuthError(errEl, 'Remplissez tous les champs.'); return; } setLoading(true, 'Connexion…'); const { data, error } = await db.auth.signInWithPassword({ email, password: pass }); setLoading(false); if (error) { showAuthError(errEl, error.message); return; } currentUser = data.user; afterLogin(); } async function doSignup() { const name = document.getElementById('signupName').value.trim(); const email = document.getElementById('signupEmail').value.trim(); const pass = document.getElementById('signupPassword').value; const errEl = document.getElementById('signupError'); errEl.style.display = 'none'; if (!name || !email || !pass) { showAuthError(errEl, 'Remplissez tous les champs.'); return; } if (pass.length < 8) { showAuthError(errEl, 'Mot de passe : 8 caractères minimum.'); return; } setLoading(true, 'Création du compte…'); const { data, error } = await db.auth.signUp({ email, password: pass, options: { data: { full_name: name } } }); setLoading(false); if (error) { showAuthError(errEl, error.message); return; } currentUser = data.user; afterLogin(); } async function doLogout() { await db.auth.signOut(); currentUser = null; showScreen('screenAuth'); } function afterLogin() { const meta = currentUser.user_metadata; const name = meta?.full_name || currentUser.email; document.getElementById('headerUserName').textContent = name; if (!apiKey) document.getElementById('apiWarning').style.display = 'flex'; showScreen('screenPatients'); loadPatients(); } function showAuthError(el, msg) { el.textContent = msg; el.style.display = 'block'; } // ── Patients ───────────────────────────────────────────────────────────── async function loadPatients() { setLoading(true, 'Chargement…'); const { data, error } = await db.from('patients').select('*, transmissions(count)').order('nom'); setLoading(false); if (error) { showToast('Erreur : ' + error.message); return; } renderPatients(data || []); } function renderPatients(patients) { const container = document.getElementById('patientsList'); if (!patients.length) { container.innerHTML = '<div class="empty-state"><div class="empty-icon">👤</div>Aucun patient enregistré.<br>Appuyez sur + pour en ajouter un.</div>'; return; } container.innerHTML = `<div class="section-title">${patients.length} patient${patients.length>1?'s':''}</div>` + patients.map(p => { const initials = (p.prenom[0]||'') + (p.nom[0]||''); const age = p.date_naissance ? calcAge(p.date_naissance) + ' ans' : ''; const count = p.transmissions?.[0]?.count || 0; return `<div class="patient-card" onclick="openPatient(${JSON.stringify(p).replace(/"/g,'&quot;')})"> <div class="patient-avatar">${initials.toUpperCase()}</div> <div class="patient-info"> <div class="patient-name">${p.nom} ${p.prenom}</div> <div class="patient-meta">${age}</div> </div> ${count > 0 ? `<div class="patient-count">${count} tx</div>` : ''} </div>`; }).join(''); } function openAddPatient() { document.getElementById('newNom').value = ''; document.getElementById('newPrenom').value = ''; document.getElementById('newDob').value = ''; document.getElementById('newNotes').value = ''; openModal('modalAddPatient'); } async function savePatient() { const nom = document.getElementById('newNom').value.trim().toUpperCase(); const prenom = document.getElementById('newPrenom').value.trim(); if (!nom || !prenom) { showToast('Nom et prénom obligatoires'); return; } const dob = document.getElementById('newDob').value || null; const notes = document.getElementById('newNotes').value.trim() || null; setLoading(true, 'Enregistrement…'); const { error } = await db.from('patients').insert({ nom, prenom, date_naissance: dob, notes, created_by: currentUser.id }); setLoading(false); if (error) { showToast('Erreur : ' + error.message); return; } closeModal('modalAddPatient'); showToast('Patient ajouté ✓'); loadPatients(); } function openPatient(patient) { currentPatient = patient; const initials = (patient.prenom[0]||'') + (patient.nom[0]||''); document.getElementById('detailAvatar').textContent = initials.toUpperCase(); document.getElementById('detailName').textContent = patient.nom + ' ' + patient.prenom; const age = patient.date_naissance ? calcAge(patient.date_naissance) + ' ans · ' : ''; document.getElementById('detailMeta').textContent = age + (patient.notes || ''); showScreen('screenPatient'); loadTransmissions(); } // ── Transmissions ───────────────────────────────────────────────────────── async function loadTransmissions() { const { data, error } = await db.from('transmissions') .select('*').eq('patient_id', currentPatient.id).order('created_at', { ascending: false }); if (error) { showToast('Erreur : ' + error.message); return; } renderTransmissions(data || []); } function renderTransmissions(txs) { const container = document.getElementById('transmissionsList'); if (!txs.length) { container.innerHTML = '<div class="empty-state"><div class="empty-icon">📋</div>Aucune transmission.<br>Commencez par dicter.</div>'; return; } container.innerHTML = txs.map((tx, i) => { const d = new Date(tx.created_at); const dateStr = d.toLocaleDateString('fr-FR', { day:'2-digit', month:'short', year:'numeric', hour:'2-digit', minute:'2-digit' }); return `<div class="transmission-card"> <div class="tx-header" onclick="toggleTx(${i})"> <div> <div class="tx-date">${dateStr}</div> <div class="tx-author">Par ${tx.auteur_nom || 'IDEL'}</div> </div> <div class="tx-arrow" id="txArrow${i}">▼</div> </div> <div class="tx-body" id="txBody${i}"> <div><div class="soap-tag tag-s">💬 S — Subjectif</div><div class="soap-text">${esc(tx.soap_s)}</div></div> <div class="soap-divider"></div> <div><div class="soap-tag tag-o">🔬 O — Objectif</div><div class="soap-text">${esc(tx.soap_o)}</div></div> <div class="soap-divider"></div> <div><div class="soap-tag tag-a">🧠 A — Analyse</div><div class="soap-text">${esc(tx.soap_a)}</div></div> <div class="soap-divider"></div> <div><div class="soap-tag tag-p">📋 P — Plan</div><div class="soap-text">${esc(tx.soap_p)}</div></div> </div> </div>`; }).join(''); } function toggleTx(i) { const body = document.getElementById('txBody' + i); const arrow = document.getElementById('txArrow' + i); const open = body.classList.toggle('open'); arrow.classList.toggle('open', open); } // ── Dictée ──────────────────────────────────────────────────────────────── function showDictee() { fullTranscript = ''; isRecording = false; updateMicUI(); document.getElementById('transcriptBox').textContent = 'Votre dictée apparaîtra ici…'; document.getElementById('transcriptBox').classList.remove('has-content'); document.getElementById('generateBtn').disabled = true; document.getElementById('generateLabel').innerHTML = '✦ Générer &amp; Enregistrer'; document.getElementById('dicteeError').style.display = 'none'; document.getElementById('dicteePatientName').textContent = currentPatient.nom + ' ' + currentPatient.prenom; document.getElementById('apiWarning').style.display = apiKey ? 'none' : 'flex'; showScreen('screenDictee'); } function initSpeech() { const SR = window.SpeechRecognition || window.webkitSpeechRecognition; if (!SR) return; recognition = new SR(); recognition.lang = 'fr-FR'; recognition.continuous = true; recognition.interimResults = true; let interim = ''; recognition.onresult = (e) => { interim = ''; let final = ''; for (let i = e.resultIndex; i < e.results.length; i++) { if (e.results[i].isFinal) final += e.results[i][0].transcript + ' '; else interim += e.results[i][0].transcript; } fullTranscript += final; const box = document.getElementById('transcriptBox'); const text = fullTranscript + interim; box.textContent = text || 'Votre dictée apparaîtra ici…'; box.classList.toggle('has-content', !!text.trim()); }; recognition.onerror = (e) => { if (e.error !== 'no-speech') stopRecording(); }; recognition.onend = () => { if (isRecording) try { recognition.start(); } catch(e) {} }; } function toggleRecording() { if (!recognition) { showToast('Reconnaissance vocale non disponible sur ce navigateur'); return; } isRecording ? stopRecording() : startRecording(); } function startRecording() { fullTranscript = ''; isRecording = true; updateMicUI(); document.getElementById('transcriptBox').textContent = '…'; document.getElementById('transcriptBox').classList.remove('has-content'); document.getElementById('generateBtn').disabled = true; recognition.start(); } function stopRecording() { isRecording = false; recognition.stop(); updateMicUI(); if (fullTranscript.trim().length > 5) document.getElementById('generateBtn').disabled = false; } function updateMicUI() { const btn = document.getElementById('micBtn'); const label = document.getElementById('micLabel'); const ring = document.getElementById('micRing'); const icon = document.getElementById('micIcon'); if (isRecording) { btn.classList.add('recording'); label.classList.add('active'); label.textContent = 'Enregistrement… appuyez pour arrêter'; ring.classList.add('pulse'); icon.innerHTML = `<rect x="8" y="7" width="3" height="10" rx="1.5" fill="#ff4757"/><rect x="13" y="7" width="3" height="10" rx="1.5" fill="#ff4757"/>`; } else { btn.classList.remove('recording'); label.classList.remove('active'); label.textContent = fullTranscript ? 'Dictée terminée' : 'Appuyez pour dicter'; ring.classList.remove('pulse'); icon.innerHTML = `<rect x="9" y="2" width="6" height="11" rx="3" fill="#4ecdc4"/><path d="M5 10v1a7 7 0 0 0 14 0v-1" stroke="#4ecdc4" stroke-width="1.8" stroke-linecap="round"/><line x1="12" y1="18" x2="12" y2="22" stroke="#4ecdc4" stroke-width="1.8" stroke-linecap="round"/><line x1="9" y1="22" x2="15" y2="22" stroke="#4ecdc4" stroke-width="1.8" stroke-linecap="round"/>`; } } async function generateAndSave() { if (!apiKey) { openApiConfig(); return; } if (!fullTranscript.trim()) return; const btn = document.getElementById('generateBtn'); btn.disabled = true; document.getElementById('generateLabel').innerHTML = '<div class="spinner"></div> Structuration…'; document.getElementById('dicteeError').style.display = 'none'; const prompt = `Tu es une aide documentaire pour infirmière libérale (IDEL) en France. À partir de cette dictée brute, génère une transmission structurée en format SOAP. Sois concis, clinique, professionnel. Vocabulaire infirmier français. Ne rajoute rien qui n'est pas dans la dictée. Si une section manque, indique "Non renseigné". Réponds UNIQUEMENT en JSON valide sans markdown : {"S":"...","O":"...","A":"...","P":"..."} S=Subjectif, O=Objectif, A=Analyse/Assessment, P=Plan Dictée : ${fullTranscript}`; try { const resp = await fetch('https://api.anthropic.com/v1/messages', { method: 'POST', headers: { 'Content-Type': 'application/json', 'x-api-key': apiKey, 'anthropic-version': '2023-06-01', 'anthropic-dangerous-direct-browser-access': 'true' }, body: JSON.stringify({ model: 'claude-sonnet-4-20250514', max_tokens: 600, messages: [{ role: 'user', content: prompt }] }) }); if (!resp.ok) { const e = await resp.json(); throw new Error(e.error?.message || 'Erreur API'); } const data = await resp.json(); const soap = JSON.parse(data.content[0].text.replace(/```json|```/g,'').trim()); const meta = currentUser.user_metadata; const authorName = meta?.full_name || currentUser.email; const { error } = await db.from('transmissions').insert({ patient_id: currentPatient.id, auteur_id: currentUser.id, auteur_nom: authorName, dictee_brute: fullTranscript, soap_s: soap.S, soap_o: soap.O, soap_a: soap.A, soap_p: soap.P }); if (error) throw new Error(error.message); showToast('Transmission enregistrée ✓'); showScreen('screenPatient'); loadTransmissions(); } catch(e) { document.getElementById('dicteeError').style.display = 'block'; document.getElementById('dicteeError').textContent = '⚠️ ' + e.message; btn.disabled = false; document.getElementById('generateLabel').innerHTML = '✦ Générer &amp; Enregistrer'; } } // ── API config ──────────────────────────────────────────────────────────── function openApiConfig() { openModal('modalApiConfig'); } function saveApiKey() { const val = document.getElementById('apiKeyInput').value.trim(); if (!val) return; apiKey = val; localStorage.setItem('idel_api_key', val); closeModal('modalApiConfig'); document.getElementById('apiWarning').style.display = 'none'; showToast('Clé API enregistrée ✓'); } // ── Helpers ─────────────────────────────────────────────────────────────── function showScreen(id) { document.querySelectorAll('.screen').forEach(s => s.classList.remove('active')); document.getElementById(id).classList.add('active'); } function openModal(id) { document.getElementById(id).classList.add('open'); } function closeModal(id) { document.getElementById(id).classList.remove('open'); } function setLoading(show, msg='') { document.getElementById('loadingOverlay').classList.toggle('show', show); if (msg) document.getElementById('loadingMsg').textContent = msg; } function showToast(msg) { const t = document.getElementById('toast'); t.textContent = msg; t.classList.add('show'); setTimeout(() => t.classList.remove('show'), 2500); } function calcAge(dob) { const d = new Date(dob), n = new Date(); let age = n.getFullYear() - d.getFullYear(); if (n.getMonth() < d.getMonth() || (n.getMonth()===d.getMonth() && n.getDate()<d.getDate())) age--; return age; } function esc(s) { return (s||'Non renseigné').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/\n/g,'<br>'); } // Close modal on backdrop click document.querySelectorAll('.modal-overlay').forEach(o => { o.addEventListener('click', e => { if (e.target === o) o.classList.remove('open'); }); }); 
